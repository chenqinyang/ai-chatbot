import { createBridgeComponent } from "@module-federation/bridge-react/v18";
import ProfileFeature from "./ProfileFeature.jsx";

export default createBridgeComponent({
  rootComponent: ProfileFeature
});
