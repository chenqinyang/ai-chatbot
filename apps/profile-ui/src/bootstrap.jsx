import React from "react";
import ReactDOM from "react-dom/client";
import ProfileFeature from "./ProfileFeature.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <main className="remote-preview">
      <ProfileFeature />
    </main>
  </React.StrictMode>
);
